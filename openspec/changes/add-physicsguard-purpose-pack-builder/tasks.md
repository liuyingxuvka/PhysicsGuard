## 1. Authority and Model Baseline

- [x] 1.1 Inventory repository coordination, dirty paths, existing starter-pack ownership, and official OpenSpec 1.6.0 identity.
- [x] 1.2 Freeze OpenSpec as an external read-only provider and keep the ten peer-modified skill entrypoints, existing templates, and expanded generator out of the edit set.
- [x] 1.3 Run a full Existing Model Preflight that reuses the existing asset owner and rejects a parallel generator boundary.
- [x] 1.4 Record the blocked project-level FlowGuard adoption upgrade and scope this change to its own executable model/module/test evidence.

## 2. Executable Behavior Models

- [x] 2.1 Replace the generated Risk Intent sample with the PhysicsGuard zero/one/many selection, composition, field-owner, fingerprint, and blocked-instantiation model.
- [x] 2.2 Replace the generated DevelopmentProcessFlow sample with the actual OpenSpec-to-model-to-module-to-focused-test lifecycle and stale-evidence broken path.
- [x] 2.3 Execute both correct models and their representative known-bad variants with the real FlowGuard 0.57.0 engine.

## 3. Manifest and Builder

- [x] 3.1 Add a content-addressed PhysicsGuard purpose-pack manifest that references reviewed existing assets without copying or rewriting them.
- [x] 3.2 Add immutable data types and canonical digest/fingerprint helpers for manifest, request, selection receipt, and instance receipt identities.
- [x] 3.3 Implement target-native applicability and complete deterministic candidate accounting.
- [x] 3.4 Implement base/no-match, single selection, strict dominance, safe composition, dependency/conflict checks, and one-owner field/surface enforcement.
- [x] 3.5 Implement placeholder/parameter resolution, canonical in-memory artifact construction, and separate selection/artifact/instance fingerprints.
- [x] 3.6 Implement PhysicsGuard-native manifest, selection, and instance validators with explicit non-physical claim boundaries.

## 4. Fixtures and Focused Tests

- [x] 4.1 Add known-good single, composed, and base-no-match fixtures.
- [x] 4.2 Add known-bad no-base, ambiguity, field-conflict, stale-digest, and unresolved-placeholder fixtures.
- [x] 4.3 Test manifest reload parity, candidate accounting, deterministic identities, strict dominance, and in-memory no-write behavior.
- [x] 4.4 Run focused tests and fix every failure in the new boundary.

## 5. Scoped Closure and Handoff

- [x] 5.1 Run syntax/import checks, focused tests, all three FlowGuard model checks, and official OpenSpec status/apply instructions for the final scoped snapshot.
- [x] 5.2 Confirm Git diff/status shows no edits to the ten dirty skills, existing templates, existing generator, OpenSpec source/install, or other peer-owned files.
- [x] 5.3 Report evidence, blockers, skipped checks, residual risk, and the exact claim boundary to the parent integration owner without installing, committing, or pushing.

## 6. SkillGuard-Neutral Projection

- [x] 6.1 Add a strict target-owned projection adapter that invokes the current PhysicsGuard selector first and emits the complete unsealed central catalog/applicability inventory.
- [x] 6.2 Bind every projected template body, artifact, builder, and validator to current native content identities without transferring PhysicsGuard semantics to SkillGuard.
- [x] 6.3 Cover exact inventory, native strict dominance, explicit base fallback, stale native manifest, and invalid target identity with focused tests.
- [x] 6.4 Cross-validate the projection with the current SkillGuard compiler/selector and confirm the comprehensive dataset-validation template remains the unique strict-dominance result.
