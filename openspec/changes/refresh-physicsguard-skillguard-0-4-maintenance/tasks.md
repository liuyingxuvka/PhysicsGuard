## 1. Governance and model freeze

- [x] 1.1 Apply the reviewed FlowGuard 0.59.0 project-record delta and verify project-audit parity.
- [x] 1.2 Update the existing PhysicsGuard suite model to freeze the ten-member same-unit, non-authoritative summary boundary and canonical simulator ownership.
- [x] 1.3 Add/update DevelopmentProcessFlow, StructureMesh, and TestMesh evidence for edit order, removed authority, public/module entrypoint parity, and independent check ownership.
- [x] 1.4 Add focused FlowGuard hazards that reject a foreign suite-parent unit, receipt-bearing summary, missing member, duplicate owner, copied editable simulator, or hidden not-run evidence.

## 2. Retire cross-unit parent authority

- [x] 2.1 Remove `.flowguard/skillguard-parent`, the frozen parent receipt inventory, and the parent contract generator without leaving aliases, readers, or executable history.
- [x] 2.2 Replace parent receipt replay with a receipt-free structural suite report that reads only source inventory/contract metadata and always declares `authoritative=false`.
- [x] 2.3 Update focused suite and post-archive tests to assert all ten family members, no parent unit/receipt/run/install fields, and no cross-unit authorization path.

## 3. Canonical simulator projection

- [x] 3.1 Update `scripts/upgrade_purpose_contracts.py` so canonical package modules own shared depth/verifier behavior while all ten semantic check ids, owners, dependencies, obligations, fixtures, and claim boundaries remain unchanged.
- [x] 3.2 Add explicit PhysicsGuard package/runtime prerequisites to consumer skill instructions and generated contract input graphs without adding target-domain depth.
- [x] 3.3 Regenerate the ten current source contracts and guard artifacts, remove satellite copied runtimes/verifiers, and verify source-generation parity.
- [x] 3.4 Capture pre-removal dataset runtime byte parity, run an isolated package/consumer entrypoint equivalence test outside the repository import path, and either remove the generated dataset bundle on pass or retain it with a concrete blocker.

## 4. SkillGuard 0.4 author state

- [x] 4.1 Refresh author adoption and the managed author block for SkillGuard 0.4 with exactly the ten `unit:physicsguard-family` members.
- [x] 4.2 Compile every target's `compiled-contract.json` and `check-manifest.json` with the installed SkillGuard 0.4 compiler and verify exact generated parity.
- [x] 4.3 Freeze ten member plans inside the one maintenance unit, proving 74 independent semantic projections, one execution owner per check, no foreign-unit dependency, and exact canonical simulator inputs.

## 5. Validation and evidence lifecycle

- [ ] 5.1 Run focused native PhysicsGuard/runtime/model tests and the affected FlowGuard suite, structure, process, and test checks.
- [x] 5.2 Run SkillGuard maintainer audit, affected/final unit validation, and clean consumer projection audits without activating or globally installing consumers.
- [x] 5.3 Run OpenSpec validation and verify every completed task against current artifacts.
- [x] 5.4 Run read-only evidence audits and exact GC planning for the family and retired parent roots; record current/release pins, candidates, and bytes without quarantine or purge.
- [x] 5.5 Perform a final peer-dirty/Git scope audit and report files, commands, evidence, skipped work, residual risks, and non-publication boundaries.
