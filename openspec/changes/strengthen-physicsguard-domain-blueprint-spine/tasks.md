## 1. Freeze scope, ownership, and current identities

- [ ] 1.1 Re-read the repository `AGENTS.md`, `docs/coordination.md`, `CODEX_COORD_DIR`, and `.codex/coord/` when present; record active peer claims before touching any production, test, prompt, model, documentation, packaging, or release path.
- [ ] 1.2 Capture a read-only baseline containing the PhysicsGuard source commit, dirty paths, current package version, installed package path/version, installed PhysicsGuard skill paths, current FlowGuard source/install/schema identities, and the SkillGuard global-registry identity; keep each identity as a separate claim.
- [ ] 1.3 Reserve only the files required by this change, exclude peer-owned paths and `.flowguard/evidence/**`, and stop on overlapping ownership instead of resetting, overwriting, or cleaning another agent's work.
- [ ] 1.4 Confirm the OpenSpec delta with `openspec validate strengthen-physicsguard-domain-blueprint-spine --strict` before implementation and treat any later requirement/design change as a reason to update this change first.
- [ ] 1.5 Freeze the canonical names `PhysicalModelBlueprint`, `PhysicalModelBlueprintReview`, and `python -m physicsguard.cli blueprint review BLUEPRINT --target-authority AUTHORITY --pretty`; reject alternate public names, command aliases, or compatibility entry points.
- [ ] 1.6 Record a component-to-validation map for every file expected to change so affected-only checks can be selected without an automatic run-all fallback.

## 2. Establish the governed FlowGuard project boundary

- [ ] 2.1 Wait until the authoritative FlowGuard source and installed projection are clean and stable, then run `python -B -m flowguard project-upgrade --root . --dry-run --json` from the PhysicsGuard repository.
- [ ] 2.2 Review the dry-run semantic diff and require the projected writes to remain limited to the declared FlowGuard adoption files, including `AGENTS.md`, `.flowguard/project.toml`, `.flowguard/adoption_log.jsonl`, and `docs/flowguard_adoption_log.md`; reject unrelated production or evidence writes.
- [ ] 2.3 Run the current FlowGuard project-upgrade command exactly once under a declared execution owner, preserve `.flowguard/evidence/**`, and record the resulting schema and source identity without introducing a fallback reader for an older adoption format.
- [ ] 2.4 Run the current FlowGuard project-audit/read-only currentness check and distinguish adoption-record currentness from executable model authority, installed FlowGuard authority, Git identity, and release identity.
- [ ] 2.5 If FlowGuard remains dirty or its public toolchain identity is not frozen, continue only with the native PhysicsGuard blueprint implementation and leave the optional FlowGuard projection explicitly blocked rather than binding to private or unstable internals.

## 3. Implement the canonical physical blueprint schema

- [x] 3.1 Add `src/physicsguard/schema/physical_model_blueprint.py` with strict Pydantic models for target identity, declared scope, provider capabilities, independent inventory entries, physical interfaces, state, effects, equations, constraints, assumptions, validity boundaries, hierarchy/refinement edges, native bindings, and review output.
- [x] 3.2 Give every modeled element a stable identifier, element kind, owning model, parent/refinement position, and deterministic order; reject duplicate identifiers, duplicate primary ownership, orphan elements, and more than one root.
- [x] 3.3 Represent every behavior-bearing physical block as typed input plus state producing a set of output plus next-state/effects, including units, reference frames, coordinate/sign conventions, time semantics, uncertainty where relevant, and declared pre/postconditions.
- [x] 3.4 Represent equations, residuals, conservation laws, constitutive relations, constraints, assumptions, operating envelopes, and failure boundaries as first-class physical semantics instead of burying them in free-form notes.
- [x] 3.5 Add explicit parent-child refinement records that state how child inputs, outputs, state, effects, equations, and validity boundaries compose into the parent contract.
- [x] 3.6 Add native binding references for source symbols/files, test cases, datasets, testbench resources, evidence receipts, project records, and reusable model-library assets without embedding those artifacts or making their formats the blueprint's own schema.
- [x] 3.7 Add review-layer, gap, coverage, affected-slice, reverse-trace, fingerprint, and safe-claim result models needed by the specifications; prohibit a caller-supplied `ready`, `complete`, or deepest-layer verdict.
- [x] 3.8 Enforce `extra = forbid`, bounded relative repository paths, explicit external references, deterministic canonical serialization, and secret-sensitive field rejection in every public schema object.
- [x] 3.9 Export only the canonical public types from `src/physicsguard/schema/__init__.py` and the package's intentional public surface; do not retain a second schema or compatibility alias.
- [ ] 3.10 Add one exact behavior-transition contract for every behavior-bearing element, referencing stable input, pre-state, output, post-state, effect, pre/postcondition, protected-failure, termination, and source-independent-oracle identities; require explicit empty state/effect sets rather than inference.

## 4. Build provider-neutral loading and canonical identity

- [x] 4.1 Add `src/physicsguard/io/physical_model_blueprint_loader.py` as the only YAML/JSON loader for the canonical blueprint schema.
- [x] 4.2 Normalize YAML and JSON into the same canonical object, sort order, content fingerprint, and review identity; test that formatting and key order do not alter identity.
- [x] 4.3 Require every provider declaration to advertise its capability and content fingerprint, and fail visibly when a required capability, adapter, target artifact, or schema version is missing.
- [x] 4.4 Keep the core contract provider-neutral: provider names may identify adapters, but no provider-specific field may become necessary for a valid blueprint.
- [x] 4.5 Reject unknown schema versions, ambiguous target identities, absolute paths outside the declared boundary, credential-looking fields, and silent loading through a legacy or fallback format.
- [x] 4.6 Add canonical YAML and JSON round-trip tests proving semantic parity, stable fingerprints, bounded paths, and lossless preservation of all physical semantics and bindings.
- [x] 4.7 Add the sole current `PhysicalBlueprintExportBundle` schema, canonical JSON materializer, and public loader; bind the exact blueprint/review/authority/behavior-contract/graph/evidence-manifest identities and exclude path, time, archive, and formatting metadata from logical identity.
- [x] 4.8 Prove repeated export from identical frozen inputs is byte-identical across different output directories and that a non-pass review remains non-pass after export/import.
- [x] 4.9 Add one compact default bundle projection and exact one-id `module`/`element`/`case`/`impact`/`reverse` deep projections; keep the full bundle on disk, include frozen layered coverage and claim boundaries, report canonical byte counts, and fail hard budgets without fallback or silent truncation.

## 5. Implement independent inventory and hierarchy review

- [x] 5.1 Add `src/physicsguard/core/physical_model_blueprint.py` with a pure, read-only reviewer that accepts the canonical blueprint plus explicitly supplied native artifact summaries.
- [x] 5.2 Build an independent target inventory from native-adapter-observed source census before trusting object-DNA coverage; keep the existing target-material inventory as the declared-consistency projection only. Reopened after synchronized-shrink audit proved the declared projection can shrink with the blueprint.
- [x] 5.3 Reconcile the independent inventory against blueprint members using exact identities and require one disposition for every inventory member: modeled, intentionally external, intentionally out of scope with reason, or unresolved gap.
- [x] 5.4 Reject self-declared completeness, missing observed source-census mappings, missing reverse model coverage, duplicate primary owners, disconnected subtrees, multiple roots, parent-child cycles, and child elements that have no traceable parent contribution. Reopened after the real-target model passed with a synchronously reduced denominator.
- [x] 5.5 Check parent input acceptance, child input provisioning, child output consumption, parent output construction, state initialization/transition/termination, effect ownership, unit compatibility, and validity-boundary containment for every refinement edge.
- [x] 5.6 Check that parent semantics are actually refined by child semantics rather than merely linked by labels; produce the first concrete non-refining edge as a review gap.
- [x] 5.7 Load hierarchy, evidence-registry, project-profile, test-file-contract, validation-depth, model-library, and model-revision data only through explicit adapters that preserve their native identities and freshness fields.
- [x] 5.8 Check bidirectional source-to-model/model-to-source, model-to-code/code-to-model, model-to-test/test-to-model, and model-to-resource/resource-to-model traceability within the declared boundary; report every unmatched endpoint and keep the native source denominator independent of caller mappings. Reopened after native case/result and source mapping remained declaration-only.
- [x] 5.9 Derive the achieved understanding layers in order from identity/inventory through physical semantics, refinement, bindings, validation evidence, and bounded reconstruction detail; stop at the first unmet layer.
- [x] 5.10 Return a deterministic `PhysicalModelBlueprintReview` containing the blueprint fingerprint, inventory fingerprint, achieved layer, deepest supported claim, first blocking gap, exact covered denominator, and no unsupported broad-completeness claim.

## 6. Provide one canonical blueprint review command

- [x] 6.1 Extend `src/physicsguard/cli.py` with exactly `python -m physicsguard.cli blueprint review BLUEPRINT --target-authority AUTHORITY --pretty`; require the authority artifact and retain only the standard output-format options already allowed by the CLI conventions.
- [x] 6.2 Make the command read-only and deterministic: it must not rewrite the blueprint, generate project adoption files, create evidence, install skills, mutate caches, or launch a validation run.
- [x] 6.3 Emit canonical machine-readable JSON by default and a human-readable `--pretty` projection derived from the same review object, preserving identical fingerprints, statuses, gaps, and claim boundaries.
- [x] 6.4 Define and test distinct exit behavior for review success, blueprint contract failure, missing/stale native bindings, adapter/capability failure, and internal execution failure.
- [x] 6.5 Remove or decline every proposed alias, shorthand, legacy command, or automatic format fallback so documentation, prompts, tests, and installed skills converge on the single command.
- [x] 6.6 Add CLI help and error text that explains the first actionable gap in plain language without claiming the target is reconstructable merely because parsing or local checks passed.

## 7. Implement affected-impact and reverse-trace analysis

- [x] 7.1 Add a typed physical-blueprint trace module under `src/physicsguard/core/` that projects hierarchy, interface, state, effect, equation, constraint, assumption, validity, source, test, dataset, resource, and evidence edges from the reviewed blueprint.
- [x] 7.2 Implement affected closure from one or more changed element identities through descendants, ancestors, interface consumers/providers, shared physical obligations, shared resources, and siblings whose parent composition may change.
- [x] 7.3 Implement reverse trace from any source symbol, test, dataset, resource, evidence item, output, state field, or physical obligation back to its owning blueprint element and ancestor chain.
- [x] 7.4 Treat an unknown seed, duplicate owner, ambiguous binding, stale fingerprint, or untyped edge as a visible bounded gap; do not substitute every model/test as an automatic run-all fallback.
- [x] 7.5 Produce deterministic summary, affected, and full projections from the same graph and include the seed identities, edge reasons, included/excluded boundary, first ambiguity, and projection fingerprint.
- [x] 7.6 Verify that changing a child interface or physical obligation selects all and only the required parent/sibling/shared consumers in focused impact fixtures.
- [x] 7.7 Add bundle-only hierarchy, interface, state-transition, affected-impact, and reverse-trace query functions that never scan the source repository, execute a provider, or repair an omitted relation; unknown, ambiguous, identity-only, and outside-scope terminals remain explicit portable-query gaps.
- [x] 7.8 Prove that every deep query selects exactly one explicit namespace/id, returns only the minimum closed slice, preserves source fingerprint/status/gaps, and cannot broaden to full-bundle output when the id is missing or the projection exceeds its byte budget.

## 8. Align current PhysicsGuard authorities and validation depth

- [x] 8.1 Add explicit adapters from the current hierarchy, evidence registry, project profile, test-file contracts, signal mapping, model-dataset validation, model library, and model-revision authorities; do not duplicate their native validators inside the blueprint reviewer.
- [x] 8.2 Modify an existing native schema only when the adapter proves a required identity, freshness, or binding cannot otherwise be expressed; route every such field change through the field-lifecycle review and direct-current replacement.
- [x] 8.3 Bind task-local model revisions to the blueprint fingerprint, selected affected slice, deepest achieved layer, and first unresolved gap so a stale blueprint cannot license current implementation or validation claims.
- [x] 8.4 Extend validation-depth and adequacy receipts to report the exact blueprint element denominator, tested obligations, evidence identity/freshness, unresolved elements, and the maximum supported claim for each element.
- [x] 8.5 Reconcile the module/equation ledger against the independent inventory and give every current registered module/equation an explicit modeled, external, out-of-scope, or unresolved disposition; never infer full coverage from a historical count.
- [x] 8.6 Require pointwise, temporal/stateful, conservation/residual, interface/unit, boundary/invalid-region, and cross-coupling evidence only where the corresponding blueprint semantics make that evidence applicable.
- [x] 8.7 Feed post-review runtime, test, replay, manual, or production misses through the existing model-miss route so the blueprint or its bindings are revised before the same broad claim is repeated.
- [x] 8.8 Preserve each native authority's own result and fingerprint in blueprint reports; a blueprint projection must not turn `not run`, `not applicable`, stale, or failed evidence into a pass.
- [x] 8.9 Require every native blueprint review and CLI invocation to consume a frozen `TargetInventoryAuthority`, while resolving adapter capability, owner, executor, supported schema, and version only from the runtime-closed current registry; expose no caller registry loader, API argument, CLI flag, compatibility reader, or fallback.
- [x] 8.10 Derive each exact provider, target, inventory id, revision, request id, boundary, raw locator/hash, and inventory result from the observable target-material snapshot through that registered adapter; do not hard-code fixture targets or hashes, and keep unknown or unreplayable adapters `unverified`.
- [x] 8.11 Give every reverse projection an explicit trace status and exact terminal input, binding, and resource identities; reject blueprint, target, inventory, or other non-terminal dead ends and any incomplete/stale source review as successful traces.
- [x] 8.12 Derive the governed inventory during review from raw target material that carries no caller-selected disposition rows, a content-derived material-revision fingerprint, and a request id derived from that revision; reconcile the result bidirectionally against authority and blueprint projections, while admitting a genuinely different valid material revision as a new target snapshot.
- [x] 8.13 Freeze all 152 members of the observed `default_module_registry()` baseline and record the exact partition as 39 previously grouped modeled members, 37 mechanically draftable unresolved members, 75 domain-judgment unresolved members, and one framework-only `DummyResidualModule`; fail if the frozen inventory and partition do not reconcile exactly, and keep the live public registry denominator at 152 throughout this patch.
- [x] 8.14 Replace `.physicsguard/module_equation_ledger.yaml` and `scripts/check_module_equation_ledger.py` directly with one current per-module semantic schema and checker; reject `evidence_level: navigation`, grouped semantic ownership, the old unresolved bucket, legacy readers, converter commands, aliases, dual emission, and alternate checkers.
- [ ] 8.15 Split and independently re-review all 39 previously modeled members as one semantic record per module; require the full FunctionBlock contract, equations/residuals, normalization, units/reference conventions, parameters, assumptions, invariants, validity/invalid regions, effects, protected failures, diagnostics, exact implementation/tests/counterexamples/examples/resources/oracles, primary owner, and stale triggers rather than preserving their historical modeled status.
- [x] 8.16 Produce bounded mechanical drafts for the 37 category-A members from current implementation, tests, and examples, label every draft non-authoritative, and require a separate semantic reviewer to accept, revise, or block each module one by one; generator output and its source tests cannot approve their own physical meaning.
- [ ] 8.17 Author the 75 category-B records through explicit domain judgment, verifying each equation or residual, units, assumptions, limitations, validity boundary, state/effect behavior, protected failures, diagnostics, and evidence applicability; do not fill missing meaning by copying neighboring modules or generic prose.
- [ ] 8.18 Keep `DummyResidualModule` in `default_module_registry()` and the public module export for this patch, classify it exactly as `supporting_framework_behavior` with `physical_claim_licensed=false`, and add one independently reviewed software-behavior/test semantic record covering its FunctionBlock behavior, implementation, tests, allowed consumers, prohibited physical claims, and stale triggers. Block it from physical-blueprint, validation-depth, physical-coverage, or user-facing physical-claim support, and require new or materially revised domain examples to use genuine low-fidelity physical modules.
- [ ] 8.19 Update `.physicsguard/project.yaml`, documentation, README commands, FlowGuard bindings, tests, and every exact consumer of the module ledger to the sole current semantic schema; keep derived family summaries navigation-only and unable to own or license module semantics.
- [ ] 8.20 Add exact good/bad tests for the frozen and live 152-member denominators, one-record-per-module ownership, old-39 no-grandfathering, category-A independent review, category-B required fields, dummy supporting classification, `physical_claim_licensed=false`, rejection of dummy-backed physical claims, preservation of its current public registry/export behavior, stale bindings, grouped rows, duplicate/missing rows, and rejection of the retired navigation schema.
- [ ] 8.21 Close software/registry semantic coverage only when all 152 live public registry members have one current independently reviewed record. Compute physical semantic coverage only from explicitly physical-claim-licensed members, exclude `DummyResidualModule` from that physical denominator, and leave public removal as a future separately authorized breaking-change candidate with caller inventory and version strategy rather than a patch task.
- [ ] 8.22 Split checker output into independently derived registry-inventory, FunctionBlock, equation-dependency, unit, constraint/valid-region, behavioral-test, counterexample, independent-oracle, and independent-review results; preserve one checker/review identity for full and exact-module projections, keep module scope unable to license global coverage, and isolate import, instantiation, test collection, case execution, oracle execution, and reviewer execution as explicit stages whose missing/not-run/fail states remain visible. License physical semantic coverage only when every required result passes for every physical member, and report the current structurally reconciled-but-semantically-blocked state without promotion.
- [ ] 8.23 Replace placeholder FunctionBlock outputs with every actual declared variable and residual output, including role/direction, complete value expression, normalization scale, diagnostic key, state/effect ownership, constructor/evaluation failures, and termination. Derive a canonical runtime port contract that compares input/output/state direction independently from ledger grouping, recursively resolve permitted pure helpers and local assignments, and reject coherent ledger-side role swaps, keyword-derived false state transitions, unresolved/cyclic helpers, and path-dependent value/scale/role/diagnostic ambiguity.
- [ ] 8.24 Correct every unit and reference convention from a registered project convention with a canonical unit/meta-unit vocabulary and dimensional-compatibility rules, including the independently audited molar-mass, heat-capacity, energy-flow, BSFC, speed, humidity-ratio, conductance, inertia, derivative, road-load, and damping defects. Reject name-suffix inference, copied template sentences, unknown/meta-unit misuse, and coherent fabrication of both a declaration and its ledger-local authority.
- [ ] 8.25 Expand every equation and residual dependency into one source semantic IR so no undefined `expected`, `calculated`, `delta`, mapped value, safe efficiency, storage term, local scale, dynamic role, diagnostic, helper return, `if`, or conditional expression remains. Preserve all reachable piecewise paths, resolve only permitted pure helpers with cycle/unresolved blocking, compare exact operators and branch guards, and bind the derived implementation IR to a separately executable expected-expression oracle rather than falling back to variable names.
- [ ] 8.26 Replace class-name-only test bindings and caller-embedded execution receipts with checker-owned execution of structured case contracts against the actual registered module in an isolated temporary working directory. Require exact good and protected-failure/counterexample cases, concrete inputs, and checker comparison of residual name, role, finite value, positive finite scale, diagnostic key, protected exception, or declared violation. Keep default execution `not_run`; a pytest that merely calls residual behavior and finishes with `assert True` MUST NOT qualify, and explicit pytest output remains separate non-licensing evidence.
- [ ] 8.27 Replace implementation-as-resource and implementation-as-sole-oracle bindings with typed physical-formula, project-convention, map/data, or explicit `not_applicable` resource dispositions and source-independent expected-value oracles. Execute every oracle case through a restricted expression evaluator or one registered independent selector, compare every finite expected/result value using a declared finite nonnegative tolerance, reject NaN/Inf inputs/results/expected/scales, and accept producer evidence only from that actual checker-owned execution—not an embedded self-hash or caller-written receipt. A module's own `residuals()` implementation MUST NOT be its sole semantic oracle.
- [ ] 8.28 Replace generic/self-authored reviewer labels, embedded hashes, ledger-carried result payloads, caller-supplied reviewer-execution mappings, and public-hash-only terminal receipts with one frozen `physicsguard.module_semantics_review_request.v1` request over the exact record, implementation, tests, example, resources, executed oracle result, all nine checker dimensions, and the sole current `.physicsguard/module_semantics_reviewer_provider_registry.json` fingerprint plus active-provider signature descriptor. Add exactly one current external entry point, `scripts/module_semantics_review_producer.py` / `physicsguard.module_semantics_review_producer.v1`, which replays that request and emits one `physicsguard.module_semantics_review_result.v1` plus one `physicsguard.module_semantics_review_receipt.v1` outside the ledger. The registry MUST freeze provider id, owner, tool fingerprint, signature algorithm, key id, and public verification key; a fixture private key MUST remain fixture-only and MUST NOT become a production trust root. The producer MUST require all eight machine-decidable dimensions to pass, invoke only the exact provider frozen from the closed registry, require that provider to sign one terminal subject containing the complete execution request, producer/provider commands, full provider result, full receipt body, exit, owner, disposition, and findings, verify that signature, compare the signed exit with the observed process exit, and derive its outer result/receipt deterministically from the authenticated terminal subject. When the production registry has no genuinely independent provider/public key, freeze that absence and remain terminal blocked. The checker MUST independently recheck the machine dimensions and frozen authority, verify the same terminal signature, reconstruct the provider verdict, and require the supplied producer result/receipt to equal the deterministic projection exactly; it MUST NOT accept a caller-rehashed outer pair, a never-signed execution fingerprint, a copied result with altered receipt id/command paths, or an expected reviewer from the supplied result. Keep ordinary checker execution `not_run`; it may validate explicitly supplied external terminal artifacts but MUST NOT invoke the producer, generate/self-sign acceptance, accept a caller provider mapping, accept a second/fallback producer/provider, or treat ledger-embedded evidence as authority.
- [ ] 8.29 Add permanent known-bad reproductions for a same-named fake module plus `assert True`, caller-authored execution evidence, coherent NaN/Inf oracle payloads and self-hashed receipts, self-authored fake reviewers, accepted producer artifacts over blocked machine dimensions, unregistered reviewer/provider mappings, stale/foreign/mismatched provider tools and registries, mismatched provider request/result/fingerprint/exit/owner, an unpublished execution fingerprint followed by complete public rehashing, a fully synthetic accepted provider+producer pair produced without executing either owner, a copied valid signed result whose receipt id or command paths are changed and rehashed, and a fixture signature checked against a different production public key. Retain helper/local-assignment/`if`/conditional-expression/local-scale/dynamic-role operator mutations, coherent input/output/state role swaps, fabricated constraints/regions, fabricated declaration-plus-unit authority, wrong units, missing equation definitions or branches, registry-only selectors, self-referential oracles, non-instantiating examples, and placeholder FunctionBlock outputs. Add one isolated closed-registry fixture whose registered provider is actually executed and whose authenticated terminal subject can produce accepted only when every binding and signature matches. Each negative reproduction must fail the exact owning dimension while that positive provider path, existing module/full equivalence, compact output, global-null module scope, and default-not-run behavior remain green.
- [ ] 8.30 Resolve the five model-exposed implementation/contract ambiguities for `BatteryPackPowerModule.sign_convention`, `ElectricMotorSimpleModule.residual_scale_efficiency`, `RadiatorSimpleModule.fan_power_optional`, `ThresholdStateCheckModule.state_tolerance`, and the unit-bearing strict-equality penalty in `MapMonotonicityCheckModule` through explicit intended behavior, direct-current implementation, exact tests, and ledger updates; remove a dead field only when field-lifecycle and public-caller evidence license removal, and normalize strict equality with the configured residual scale rather than a dimensionless literal.
- [ ] 8.31 Repair the five invalid example bindings for `RatioModule`, `SaturationModule`, `SumModule`, `ThermalMassStepModule`, and `ThermalConductorModule`; require the selected example to instantiate the exact module successfully and bind the intended behavior rather than merely containing its class name.
- [ ] 8.32 Keep the sole semantic-ledger checker token-bounded: default JSON reports the nine aggregate dimensions, counts, blocked module ids, per-record status/count, and first gap without embedding every finding; an explicit exact-module detail request returns that module's complete findings, unknown modules fail visibly, and compact/detail projections preserve the same underlying review identity without introducing another checker or result authority.
- [ ] 8.33 Require every module semantic record to project the exact behavior contract `Input + PreState -> Output + PostState + Effect`, protected failures, termination, and independent oracle from its existing verified ports, semantic IR, structured cases, and oracle bindings. Do not invent equations or state: when implementation/tests/docs cannot establish a member, keep that member's first gap and independent-semantics status non-pass.

## 9. Add exhaustive fixtures and focused tests

- [x] 9.1 Add `tests/test_physical_model_blueprint_schema.py` covering valid construction, strict fields, target identity, independent inventory, one-root ownership, typed physical semantics, refinement edges, native bindings, and caller-supplied readiness rejection.
- [x] 9.2 Add `tests/test_physical_model_blueprint_review.py` covering inventory reconciliation, hierarchy composition, physical-semantic refinement, binding closure, derived depth, first-gap reporting, and safe claim boundaries.
- [x] 9.3 Add `tests/test_physical_model_blueprint_cli.py` covering the single canonical command, YAML/JSON parity, pretty/machine parity, deterministic output, exit codes, no writes, and absence of aliases/fallbacks.
- [x] 9.4 Add `tests/test_physical_blueprint_impact_trace.py` covering descendant, ancestor, sibling, shared-obligation, shared-resource, reverse-trace, ambiguity, and minimal affected-closure cases.
- [x] 9.5 Add `tests/test_physical_model_blueprint_provider_neutral.py` with at least two provider shapes and one non-Python target, proving the core schema and review result do not depend on a programming language or provider-specific field.
- [x] 9.6 Add canonical YAML and JSON fixtures plus `templates/physical_model_blueprint.yaml`, and add a complete example blueprint at the target root `examples/testfile_contracts/pump_loop/pump_loop_physical_blueprint.yaml` so its forward-relative local paths bind the existing source, test, dataset, and evidence artifacts without inventing new physical facts.
- [x] 9.7 Add known-bad fixtures for two roots, duplicate ownership, orphan child, missing child input, unconsumed child output, incompatible unit/frame/time semantics, unowned state/effect, weaker child validity boundary, missing equation/constraint, stale source/test/resource binding, and unknown schema version.
- [x] 9.8 Add known-bad fixtures for incomplete inventory, ambiguous reverse trace, missing provider capability, provider-specific required field, external path escape, credential-like content, false self-readiness, and pointwise-only evidence for a stateful obligation.
- [x] 9.9 Assert that every bad fixture yields the exact bounded diagnostic and no broad claim, while every good fixture reports the exact denominator and deepest supported layer.
- [x] 9.10 Run focused schema/reviewer/CLI/impact/provider tests after each implementation slice and keep the repository-wide suite for the frozen integration snapshot.
- [x] 9.11 Add an exact adversarial fixture that leaves raw target material unchanged while shrinking the blueprint, inventory, frozen authority projection, and caller-created registry together; prove that the registry cannot enter the public API and that replay restores the full raw-material denominator. Also prove that a new explicit non-fixture material revision is admitted through the same generic adapter.
- [x] 9.12 Before graph compilation, run exactly one canonical review from the exact blueprint, frozen target-inventory authority, blueprint artifact root, and authority artifact root on every affected and reverse query; require the supplied review to equal that passing result field-for-field. Prove real incomplete, blocked, stale binding, foreign, caller-rehashed-pass, raw-material-changed, missing-context, and valid-current cases fail or pass atomically with no silent repair. Keep acceptance counts derived from the live material inventory rather than a historical literal.
- [ ] 9.13 Add exact good/bad tests for behavior-transition contracts: stateless explicit-empty state, stateful pre/post-state, effect ownership, protected failure, termination, missing post-state, missing failure, and implementation-as-sole-oracle rejection.
- [x] 9.14 Launch one isolated subprocess from a temporary directory containing only an exported bundle plus the installed package, answer independently frozen hierarchy/interface/state/impact/reverse fixtures, and prove repository absence, path independence, deterministic bytes, bounded missing-artifact gaps, and no review-status promotion.
- [x] 9.15 Measure canonical bytes for the complete bundle, default status projection, and representative module/element/case/impact/reverse projections; assert the default fits its hard budget, each deep route fits its declared budget, and no ordinary query serializes the complete bundle.

## 10. Update the FlowGuard development model and code/test bindings

- [ ] 10.1 Add a task-owned FlowGuard model for the blueprint lifecycle under `.flowguard/physical_model_blueprint/`, with FunctionBlocks for load, identity, inventory, hierarchy, physical semantics, refinement, native bindings, depth derivation, impact trace, and claim emission.
- [ ] 10.2 Express every FunctionBlock as input plus state producing a set of output plus next-state/effects, and encode hard invariants for one root, one primary owner, complete inventory disposition, compositional refinement, exact binding freshness, and no self-declared readiness.
- [ ] 10.3 Add executable model checks for good scenarios, canonical counterexamples, conformance replay, loop/stuck states, progress/fairness where applicable, and contract/refinement checks before using the model to justify production structure.
- [ ] 10.4 Update `.flowguard/model-regression-manifest.json` and `.flowguard/model_code_ledger.yaml` with exact model-to-code-to-test owners for every blueprint obligation; reject unmapped implementation or test files.
- [ ] 10.5 Update the PhysicsGuard skill-suite mesh and prompt-load graph so all ten skills consume the right blueprint slice and no skill duplicates the canonical author/reviewer role.
- [ ] 10.6 Run only the affected FlowGuard model owners while source is changing; after the integration snapshot is frozen, publish one current `ModelRevisionSet` transaction rather than hand-editing snapshots, activations, or result files.
- [ ] 10.7 Require the final FlowGuard authority to contain the new model, non-empty behavior commitments, the required public surfaces and field lifecycles, current test evidence, and a topology built after all result producers.
- [ ] 10.8 Keep `.flowguard/evidence/**` out of source edits and release staging, and never treat cached evidence, a progress log, or a stale model snapshot as current authority.

## 11. Upgrade the ten PhysicsGuard skill prompts and route contracts

- [x] 11.1 Upgrade `physicsguard-candidate-model-blueprint` as the sole route that authors or fully reviews a `PhysicalModelBlueprint`; require the canonical CLI, independent inventory, hierarchy/refinement checks, binding closure, fingerprint, achieved depth, first gap, and safe claim in its output.
- [x] 11.2 Upgrade `physicsguard-model-understanding-preflight` to establish target identity, declared boundary, independent inventory inputs, provider capabilities, and the first useful understanding layer without claiming that planning output is a complete blueprint.
- [x] 11.3 Upgrade `physicsguard-project-evidence-registry` to bind evidence/resource identities and freshness to exact blueprint elements and to return explicit missing/stale gaps rather than a project-wide pass.
- [x] 11.4 Upgrade `physicsguard-test-file-contract-review` to bind deterministic files, fields, datasets, testbench inputs/outputs, and expected evidence to the relevant blueprint interfaces, states, and physical obligations.
- [x] 11.5 Upgrade `physicsguard-signal-mapping-review` to consume the affected interface slice and validate source/target variables, units, frames, conversions, time semantics, and downstream blueprint consumers.
- [x] 11.6 Upgrade `physicsguard-model-dataset-validation` to report per-blueprint-element dataset/simulation coverage, validation mode, validity boundary, residual/oracle, evidence fingerprint, and unsupported claims.
- [x] 11.7 Upgrade `physicsguard-model-library` to index and select reusable assets by blueprint fingerprint, physical obligation, profile, validity boundary, testbench, and verified reuse limits rather than by loose similarity.
- [x] 11.8 Upgrade `physicsguard-project-adoption` to record the project's current blueprint identity, declared scope, native authorities, and adoption boundary without making adoption itself a completeness verdict.
- [x] 11.9 Upgrade `physicsguard-audit-closure` to consume whole or affected blueprint review receipts and allow only claims supported by the current denominator, depth, native evidence, and unresolved-gap boundary.
- [x] 11.10 Upgrade `physicsguard-ai-debugging` as a mixed-route coordinator that consumes blueprint impact/reverse trace and delegates to the minimum owning PhysicsGuard routes; prohibit it from becoming a universal parent or a second blueprint reviewer.
- [ ] 11.11 For each of the ten skills, update its `SKILL.md`, `agents/openai.yaml`, route capsule, native protocol/depth reference only when required by that route, five guard-model JSON fixtures, `.skillguard/contract-source.json`, and release-time runtime-requirements fingerprint.
  - Current implementation status: the ten route prompts/references, five generated guard-model files, native blueprint obligations, and exact SkillGuard component/owner mappings are regenerated and focused-test green. The release-time `runtime-requirements.json` version fingerprint remains deliberately open until the patch version is frozen, so 11.11 is not complete yet.
- [x] 11.12 Preserve the current compact-loading contract for every skill: concise entry prompt, conditional references, maximum reference depth one, no new skill name, no alias route, and no compatibility copy of retired instructions.
- [x] 11.13 Add route-specific known-good and known-bad prompt cases proving each skill owns only its domain semantics and that all blueprint creation/full-review requests converge on `physicsguard-candidate-model-blueprint`.
- [x] 11.14 Correct the candidate blueprint prompt and native protocol to use exactly `python -m physicsguard.cli blueprint review BLUEPRINT --target-authority AUTHORITY --pretty`; update prompt regression so the obsolete authority-less command is a known bad rather than the protected path.
- [ ] 11.15 Update only the affected candidate-blueprint and audit-closure prompt contracts so full review may materialize/hand off the deterministic portable bundle and closure may consume its frozen status without treating bundle presence or AI answers as native execution, fresh evidence, or high-fidelity reconstruction.

## 12. Rebuild prompt depth and SkillGuard author authority

- [x] 12.1 Update `src/physicsguard/skill_execution_depth.py` and its fixtures so blueprint authoring/review, affected-slice consumption, and closure obligations are represented explicitly for the ten current skill identities.
- [ ] 12.2 Update the current purpose-contract generator or source declarations, then regenerate prompt artifacts through the declared generator; do not hand-maintain a second prompt authority.
  - Current status: the generator source is updated, but the latest family-owner and route-specific input changes have intentionally not been projected into the ten generated contracts while FlowGuard authority is moving. Official regeneration remains part of 12.14 and 12.17.
- [ ] 12.3 Regenerate and validate the prompt-load graph and skill-suite mesh, checking route reachability, conditional-reference depth, duplicate responsibility, orphan prompt blocks, and cross-skill output handoffs.
  - Current status: the prior graph remains useful source history, but the suite mesh must be regenerated to replace the old 84-owner topology with the current 85-owner topology on the frozen toolchain.
- [ ] 12.4 Resolve the single registered `unit:physicsguard-family` maintenance unit and its exact ten member skills from the SkillGuard global registry; freeze the unit id, ten member ids, each member's native owner and receipt subjects, semantic checks, covered obligations, dependencies, and one final full-validation execution owner for the unit.
- [ ] 12.5 Compile every maintained source file into an exact SkillGuard content component and block on an ambiguous or unmapped file instead of selecting every check.
- [ ] 12.6 Run only affected member-owned native checks under SkillGuard supervision as each prompt changes and reserve exactly one final full-validation owner for the frozen `unit:physicsguard-family`; do not execute that full owner in this section. The post-archive release runbook is its sole execution site.
- [ ] 12.7 Require current member-owned receipts for schema validation, prompt load, route behavior, guard-model cases, contract-source agreement, and package projection; keep each member's evidence subject and native owner exact even though all ten belong to the same maintenance unit.
- [ ] 12.8 Verify that installed consumer skills contain no `.skillguard` records, author receipts, execution-owner projections, source-only fixtures, or private registry paths.
- [x] 12.9 Regenerate all ten route-capsule prompt/reference fingerprints and the prompt-load graph through the repository's existing official generator owner, then require the official checker to report zero stale prompt or conditional-reference findings without running SkillGuard or a full validation owner in this repair slice.
- [x] 12.10 Replace the repository-owned consumer comparison algorithm with one thin exact-ten checker that path-validates and dynamically loads the current installed SkillGuard `consumer_distribution_plan` and `audit_consumer_distribution`, then compares each full source plan/release/file list to that audit without writing.
- [x] 12.11 Make `verify_guard_simulation_readiness.py` consume the exact shared installed-suite result once and remove its duplicate consumer manifest/hash/inventory implementation.
- [x] 12.12 Add one exact-ten suite coordinator over SkillGuard's public target-installation API: plan all, prepare all, verify all, then activate; on activation failure call SkillGuard rollback for prior activations in reverse order, with no PhysicsGuard-owned lock, receipt, projection, or fallback installer.
- [x] 12.13 Update generator source so every `skill/<id>/.skillguard/**` member is `source_only` and FlowGuard/SkillGuard versions are resolved from explicit current authorities rather than historical literals; block generation on FlowGuard project/import drift.
- [ ] 12.14 After the concurrently changing FlowGuard authority is frozen, rerun the official generator owner, inspect the ten regenerated impact plans and contracts, and only then execute the still-pending SkillGuard validation and installation steps; this source-only repair did not run the generator.
- [x] 12.15 Replace ten duplicated maintenance-input invalidations with one family-level distribution authority owner; retain 84 domain/model owners, place the one process owner in exactly one registered member without giving it physical-domain claim authority, and make the root TestMesh static audit execute once rather than once per member.
- [x] 12.16 Bind the full route-specific `PhysicalModelBlueprint` implementation and tests to the five already existing direct route owners for candidate review, signal mapping, project evidence, dataset/validation, and audit closure instead of copying that surface into all ten members.
- [ ] 12.17 During the frozen 12.14 generation, require the generated root topology to contain exactly 85 owners, one family maintenance projection, no maintenance-only inputs in the other nine member owners, exact route-specific blueprint mappings, and complete `.skillguard/**` plus `guard-model/**` source-only classification before any owner receipt is accepted.

## 13. Prove the physical-domain capability on a representative external target

- [x] 13.1 Select one independently owned representative external physical target from the maintained examples or add one bounded fixture that contains a non-trivial parent/child physical hierarchy, interfaces, state/effects, equations or residuals, assumptions, validity boundaries, implementation/workflow bindings, tests, datasets, evidence, oracles, and resources.
- [x] 13.2 Build the target inventory independently from native observation of its project files, declared interfaces, tests, datasets, testbench resources, and metadata before authoring the target blueprint; preserve every observed member in the denominator even when caller expected subsets, mappings, or affected scope omit it. Reopened because the FMI observer emitted only caller-selected member and variable subsets.
- [x] 13.3 Author one canonical physical blueprint for that external target and bind every in-scope physical element to its parent, typed inputs/outputs/state/effects, physical semantics, validity boundary, native implementation/workflow, tests, resources, and current evidence.
- [x] 13.4 Review the target through the public loader, canonical CLI, native reviewer, impact trace, reverse trace, and depth derivation with no private flag, hard-coded fixture exception, skipped adapter, or alternate success path.
- [x] 13.5 Exercise forward affected analysis from representative interface, equation/constraint, state, validity, dataset, and resource changes, and exercise reverse trace from representative outputs, diagnostics, tests, and evidence back to their owning physical elements.
- [x] 13.6 Feed every qualification miss back into the canonical schema, reviewer, owning PhysicsGuard skill, adapter, or target binding rather than weakening the target or suppressing the diagnostic.
- [x] 13.7 Rerun the external-target review until its declared boundary passes or the release report names the exact unresolved gaps and achieved depth; state only the bounded capability claim supported by this target.
- [x] 13.8 Add a guard test proving PhysicsGuard software modules, CLI handlers, prompts, and skills cannot be relabeled as physical elements to satisfy representative external-target qualification.
- [x] 13.9 Freeze one real non-self target whose subject, target material, engineering semantics, observations/tests, and evidence ownership pre-exist and remain independent of PhysicsGuard implementation, prompts, checker, examples, and qualification fixtures; keep unavailable artifacts as exact gaps. Reopened until the target's independent source census and source-to-model mappings are part of the qualified result.
  - Evidence: official Modelica Reference-FMUs v0.0.40 BouncingBall FMI 3.0 source/release/license identities, exact release/FMU/member hashes, restricted source-bound oracles, five cases, 61-member target material, authority, and layered blueprint are retained under `examples/external_targets/reference_fmus_v0_0_40_bouncing_ball/`; third-party bytes remain external and absence reports `external_resource_not_run`.
- [x] 13.10 Review that real target through the same public loader, authority, canonical reviewer, affected/reverse trace, and portable export path without a fixture whitelist, private flag, or target-specific success owner. Reopened until wrong-equation, false observed-value, and synchronized-shrink mutations fail.
  - Evidence: the public CLI with explicit material root passed 61/61 through `static_blueprint`; generic FMI replay produced the restitution and stop outcomes, while bundle impact/reverse queries stayed inside the hard projection budget with identity-only byte gaps preserved.
- [x] 13.11 Export the representative and real-target results, then run an internal isolated-bundle integrity qualification from each bundle alone against declared hierarchy, interface, state, impact, reverse-trace, source-census, source-to-model, and native-result slices; report exact status and every first gap without promoting either target beyond its current evidence. No external AI or consumer answer exercise is part of this task.
  - Evidence: the bundle verifier consumed only the exported bytes, matched identity and declared slices, and preserved `observed_at_export_unlicensed` plus `portable_query_identity_only_terminal`; external-agent qualification was intentionally not run because it is outside the product boundary.
- [x] 13.12 Keep the existing pump-loop target as regression evidence only unless its subject and evidence ownership can be proven independent; do not let a richer self-authored fixture substitute for 13.9 real-target qualification.

## 13A. Close the real-target object-DNA trust boundary

- [x] 13A.1 Add explicit `declared_consistency` and `object_dna` understanding targets; derive and report declared-consistency status separately from object-DNA readiness.
- [x] 13A.2 Add one provider-neutral source-census result contract and make the FMI adapter emit every discovered archive member and XML variable independently of expected-member subsets. Require typed FMI name, value reference, causality, variability, unit, derivative/reinit, and target port/quantity/unit/state-role/conversion correspondence so a coherent variable remap cannot pass. Reopened after the independent `v`/`g` swap attack passed.
- [x] 13A.3 Add exact source-to-model mappings and terminal dispositions, validate every mapping target, require full observed-source coverage and reverse behavior-bearing model coverage in object-DNA mode.
- [x] 13A.4 Bind object-DNA behavior cases to an independently observed complete native-case universe, exact native case results, and port-to-native-value mappings with tolerances; treat caller-selected case subsets, observed values, and pass verdicts as claims rather than proof. Reopened after the independently observed five-case target still qualified when the candidate case set was coherently reduced to one.
- [x] 13A.5 Bind every claimed restricted semantic fact to an exact independently observed selector, expression/AST/IR fragment, or explicit unresolved disposition and fail on normalized semantic mismatch without claiming universal physical truth. Reopened after changing `sem.impact.trigger` to a contradictory condition still qualified because a whole-file hash was too coarse.
- [x] 13A.6 Include scope and selected closure in review identity; preserve the complete global denominator and explicit outside-scope members in affected reviews.
- [x] 13A.7 Carry a compact source census, mappings, native-result links, understanding target, both readiness statuses, and first gap in the portable bundle.
- [x] 13A.8 Add regressions proving synchronized shrink, expected FMI subset shrink, typed variable remapping, native-case-universe shrink, wrong selector/expression, wrong equation, and false claimed observed value fail; prove lightweight provider-neutral targets remain usable without being promoted to object DNA. Reopened until the independent `v`/`g`, 5-to-1 case, and wrong-impact-trigger attacks are all blocked.
  - Evidence: an independent read-only replay rebuilt the current candidate fingerprints after each mutation and still hard-blocked the coherent `v`/`g` target/port swap, five-to-one native-case shrink, and wrong `sem.impact.trigger` condition. The same replay closed the frozen Reference-FMUs v0.0.40 FMI 3.0 BouncingBall boundary at 41/41 source members, 17/17 exact selectors, 5/5 native cases, zero gaps, and 11 passing focused attack/object-DNA/bundle/trace tests. This licenses only that exact target boundary, not universal FMI or physical truth.

## 14. Build FlowGuard software self-DNA and reduce architecture from it

- [ ] 14.1 After the authoritative FlowGuard source, install, public interface, and PhysicsGuard adoption identity are frozen, derive an independent whole-repository denominator from the exact PhysicsGuard source snapshot. Include every release-governed behavior, tracked file, public import, CLI command/subcommand, script entry point, package/build input, ten skill routes and prompts, OpenSpec intent, documentation/example/template, test/fixture, configuration, model source, resource/evidence owner, install surface, and release surface, including all non-Python members.
- [ ] 14.2 Give every denominator member exactly one terminal disposition and one primary owner; record supporting, generated, externally owned, deliberately excluded, retired, and unresolved members explicitly, and block whole-DNA qualification on any missing, duplicate, or ambiguous disposition or owner rather than shrinking the inventory.
- [ ] 14.3 Build exactly one FlowGuard software-DNA root for the complete repository and attach recursively compositional children for public product/API/CLI behavior, native physical-blueprint and module behavior, the ten agent skill/prompt routes, development/model/test/evidence workflows, and distribution/install/release behavior. Model the behavior that authors and reviews `PhysicalModelBlueprint` only as a product-function subtree; never use a physical-target blueprint as the self-DNA root or denominator.
- [ ] 14.4 Express every behavior-bearing node as `Input × State -> Set(Output × State)` with explicit preconditions, postconditions, failures, effects, termination, child-input sources, child-output consumers, parent state, sibling connections, and terminal dispositions; reject label-only parent/child links.
- [ ] 14.5 Bind every node bidirectionally to governing intent, Python or non-Python implementation and public entry point, tests/counterexamples/oracles/current evidence, resources/schemas/prompts/templates/datasets/configuration/provider dependencies, and parent/child, sibling, call, data, state, installation, and release topology.
- [ ] 14.6 Derive and test the seven stable provider-neutral software-DNA layers in exact order: evidence qualification; implementation inventory; traceability; independent semantics; model-code-test binding; resource-oracle binding; and static blueprint readiness. Stop at the first gap and reject caller-declared or summary-derived readiness. Treat the whole-repository denominator as implementation-inventory evidence, root/child topology plus intent and affected/reverse relations as traceability obligations, and FunctionBlocks as independent-semantics obligations; do not invent project-specific substitute layers.
- [ ] 14.7 Compile one deterministic affected index and reverse index across behaviors, files, entry points, intents, tests, evidence, resources, child models, installation, and release surfaces. Prove exact seed, inclusion reason, terminal grounds, ambiguity handling, and stable projection fingerprints with known-good and known-bad fixtures.
- [ ] 14.8 Make ordinary maintenance consume the smallest closed affected slice plus required ancestors, connected siblings, and shared consumers. Select whole-DNA review only for initial backfill, an explicit whole-boundary request, scope ambiguity that blocks bounded work, final architecture-reduction closure, or the frozen release gate; never fall back to repository scanning, cached discovery, arbitrary ownership, or automatic run-all.
- [ ] 14.9 After deriving an exact static result, materialize the canonical software-DNA bundle, report, and affected/reverse indexes to an explicitly selected artifact root outside the PhysicsGuard repository. Verify before writing that the resolved output is outside the repository, prove repeated materialization from the same frozen snapshot is deterministic and cannot change the denominator or fingerprint itself, preserve non-pass static status without promotion, and record materialization as a separate gate outside the seven layers.
- [ ] 14.10 Keep repository-local model sources and generators in the denominator but retain no in-repository canonical bundle, cache authority, compatibility export, legacy reader, alias, fallback, second current head, or hand-authored activation; publish current model authority only through the official FlowGuard transaction after all exact producers are current.
- [ ] 14.11 Use the qualified whole-repository model, indexes, ledgers, and runtime evidence to enumerate duplicate-looking handlers, adapters, prompt routes, validators, public entry points, schemas, and obsolete branches. For each candidate record observable contract, sole owner, consumers, affected and reverse closure, code/test/intent/resource bindings, parity oracle, proof status, proposed retain/merge/delete/defer action, migration impact, and rollback boundary.
- [ ] 14.12 Mark a candidate proof-ready only when every required identity and parity obligation in 14.11 is current and unambiguous. Retain or defer size-only, name-only, token-only, stale, or incompletely modeled candidates; do not convert them into speculative cleanup work.
- [ ] 14.13 Route each proof-ready public command, facade, package, or large-module change through StructureMesh and each field/schema removal through field-lifecycle evidence. Freeze the surviving primary owner and direct-current disposition before editing.
- [ ] 14.14 Apply only proof-ready behavior-preserving reductions with narrow owned diffs, delete the replaced path in the same rollbackable change, and add no compatibility shim, alias, fallback handler, legacy reader, alternate command, or duplicate success route.
- [ ] 14.15 After each accepted reduction, rerun its exact affected software-DNA owners and parity tests, rebuild affected/reverse projections, and require the independent denominator, ownership, topology, observable behavior, and earlier licensed depth to remain current.
- [ ] 14.16 On the frozen source snapshot, rerun whole-DNA static qualification, then separately materialize the canonical external bundle and manifest binding bundle fingerprint, denominator fingerprint, source commit, package version, and claim boundary. Do not count materialization, package tests, installation, Git/tag/GitHub release, or release-asset publication as static-depth evidence; keep the bundle outside the wheel, source distribution, installed package, and installed consumer skills.
- [ ] 14.17 Produce the final architecture/self-DNA report listing the seven stable layer results, first gap, deepest licensed static layer, denominator and owner coverage, accepted reductions, retained/deferred candidates and reasons, token/load and test impact, external materialization identity, and every remaining ambiguity. Report current test execution, package, installation, Git/tag/GitHub release, and release-asset lifecycle states separately and never broaden static readiness into those claims.

## 15. Keep FlowGuard projection delayed and bounded

- [ ] 15.1 Define a project-level FlowGuard projection contract that consumes only a completed native `PhysicalModelBlueprintReview` and preserves target, blueprint, inventory, projection, and native-evidence fingerprints without recomputing physical truth.
- [ ] 15.2 Keep any executable FlowGuard adapter outside `src/physicsguard`; confirm the built PhysicsGuard package imports, reviews blueprints, and runs its CLI when `flowguard` is absent.
- [ ] 15.3 Do not implement the executable projection until FlowGuard has a clean, published, tested public provider/composition API and matching installed identity; record this as a deliberate integration gate, not a fallback.
- [ ] 15.4 When the gate is met, implement the adapter only in the project-level FlowGuard integration area and add `tests/test_physical_blueprint_flowguard_projection.py` for status/fingerprint preservation, capability negotiation, and no domain recomputation.
- [ ] 15.5 If the gate is not met at release freeze, ship and validate the native PhysicsGuard capability plus its stable projection contract, leave the optional adapter absent, and state that absence precisely without weakening any native blueprint claim.

## 16. Update public documentation and examples

- [x] 16.1 Update the repository README and relevant PhysicsGuard documentation to explain in plain language that the blueprint is the target's layered physical DNA, what each derived depth means, and what the blueprint cannot claim without current bindings/evidence.
- [x] 16.2 Document the single `blueprint review` command with canonical YAML and JSON examples, machine and pretty output, exit meanings, first-gap interpretation, affected/reverse trace use, and the no-write guarantee.
- [x] 16.3 Document the ownership boundary: PhysicsGuard owns physical-domain semantics and qualification; FlowGuard owns only the reusable behavior/state/modeling spine and an optional later project-level projection.
- [x] 16.4 Document lightweight, affected, and full usage as projections of one canonical blueprint rather than three competing schemas or workflows.
- [x] 16.5 Document how existing software, non-Python software, hardware/experimental rigs, simulation models, and mixed workflows declare provider adapters without changing the core blueprint schema.
- [ ] 16.6 Update the changelog with the native blueprint capability, per-module semantic-ledger closure and retained dummy `supporting_framework_behavior` boundary, skill-route changes, representative external-target qualification result, seven-layer FlowGuard static software-DNA result, architecture reductions, external DNA materialization and release-asset lifecycle boundaries, validation boundaries, and any deliberately deferred FlowGuard projection or future dummy breaking-change candidate.

## 17. Complete implementation evidence and close OpenSpec before the release freeze

- [x] 17.1 Run the new schema, loader, reviewer, CLI, impact, provider-neutral, and representative external-target test files directly and fix every failure at its owning layer.
- [x] 17.2 Run affected existing PhysicsGuard tests for hierarchy, evidence registry, project profile, test-file contracts, signal mapping, validation depth, model library, model revision, CLI, and skill execution depth.
- [ ] 17.3 Run all affected FlowGuard model checks and inspect every counterexample; revise architecture or contracts instead of weakening hard invariants to make the checks green.
- [ ] 17.4 Run all affected SkillGuard member checks for the ten registered skills under their exact native owners and verify each receipt subject, source, dependencies, toolchain, environment, and terminal status; reserve one final full-validation owner for their single `unit:physicsguard-family` maintenance unit.
- [ ] 17.5 Run the canonical representative and real non-self external physical-target reviews, deterministic portable export and internal bundle-integrity/query checks, whole-scope impact/reverse-trace checks, and close the live 152-member per-module semantic ledger with explicit behavior contracts and the dummy excluded from physical claims.
- [ ] 17.6 Use the provisional whole-repository software-DNA result to complete the proof-gated architecture-reduction pass and every affected parity check. Treat this result as implementation evidence only; it is not the final release DNA because OpenSpec archival still changes the repository.
- [ ] 17.7 Build a provisional package in an isolated output directory and inspect wheel/sdist contents for unintended files, private paths, secrets, author receipts, source-only fixtures, and accidental FlowGuard runtime dependencies; do not call this the release artifact.
- [ ] 17.8 Re-run `openspec validate strengthen-physicsguard-domain-blueprint-spine --strict` and use the OpenSpec verify route to compare every requirement and scenario against implementation evidence. Keep execution, installation, Git, tag, release, and asset states separate from product-behavior verification.
- [x] 17.9 Run focused no-bytecode/no-cache tests for installed consumer drift and read-only behavior, current-authority failure, exact-ten phase ordering, activation rollback, generator authority resolution, `.skillguard/**` classification, and readiness de-duplication; keep generator execution, installation, version changes, and full owner validation out of this repair slice.
- [ ] 17.10 After 17.8 passes, sync the accepted delta specs into main specs and archive `strengthen-physicsguard-domain-blueprint-spine` before freezing the release candidate. Freeze and record the archived intent-artifact digest at that moment. The archived proposal, design, specs, and task list become an immutable intent snapshot: post-archive lifecycle status is written only to an out-of-repository release receipt and final report, never by editing archived checkboxes. OpenSpec archival is an intent-source change and therefore cannot occur after the final DNA, package, commit, tag, or release freeze.

## 18. Post-archive release runbook (status is external; never edit the archived checklist)

1. Compute the release number from the frozen current semantic version and increment only the third component. Update every governed version authority, including package metadata, runtime version constant, changelog, skill runtime requirements, the tracked release-role plan, and model/release source identities.
2. Prepare only the current model and binding sources needed by repository generators; this preparation is not final model authority. Regenerate every tracked in-repository output that can affect the denominator before final DNA computation: the ten SkillGuard contract sources/compiled contracts/check manifests, the exact 85-owner test topology, route/prompt projections, consumer source projections, FlowGuard project records, and any version-derived model or release source. Prove source-only boundaries and exact owner mapping.
3. On those fully generated post-archive bytes, independently rebuild the complete denominator and ownership table, recompute affected/reverse indexes, refresh model-code-test/intent/resource/topology bindings, rerun the seven static layers, and publish the sole current FlowGuard model authority. Model-authority records and execution receipts are predeclared derived evidence outputs, not inputs to their own logical fingerprint; after publication rerun the inventory audit and require that only those declared outputs appeared and the logical DNA identity remained unchanged.
4. Materialize the canonical logical software-DNA bundle twice from that exact static result to isolated roots outside the repository. Require identical logical identity, zero repository writes, complete denominator/root/child/index/seven-layer/claim contents, no output-path self-reference, and no promotion of static status into execution or release claims.
5. Freeze source, dependencies, toolchain, model, affected plan, test inventory, and the one repository-suite plus one `unit:physicsguard-family` full-validation owner. Confirm zero descendants from interrupted launchers, then execute those two final owners exactly once in the foreground. Run final FlowGuard checks, reduction parity, representative external-target review, package-content preview, and archived-OpenSpec currentness only under their distinct exact owners.
6. Review and stage only owned paths; preserve peer work and exclude `.flowguard/evidence/**`, caches, build leftovers, credentials, and private coordination data. Create a local candidate commit but do not push, tag, or publish it.
7. From an isolated clean checkout of that immutable local commit, build and inspect the final wheel/sdist and clean ten-skill consumer asset. Create an out-of-repository commit-binding manifest that binds the already verified logical DNA/tree fingerprint to the exact commit SHA, package/version identities, asset roles, and checksums without adding the commit SHA to the logical DNA fingerprint. If build or inspection fails, abandon the candidate, return to source repair, rerun affected and required final gates, and create a new candidate commit.
8. Install the exact candidate artifacts into isolated Python and skill roots first, verify package import/version/schema/CLI/YAML-JSON parity/no-FlowGuard-runtime behavior and ten consumer identities, then synchronize the authorized local package and skills through the current SkillGuard target-installation API. Run only read-only installed-currentness checks afterward; an install failure returns to the candidate-repair path and never creates a remote release.
9. Recheck remote branch and tag ownership and CI prerequisites. Only then push the exact candidate commit, create and push its patch tag, publish the GitHub Release, and upload the package/source, clean consumer, canonical logical DNA, commit-binding manifest, checksums, and release notes in their declared roles.
10. Download every published asset and verify its digest. Compare remote commit, tag target, GitHub Release, assets, local installation, installed skill projection, external-target qualification, model authority, seven-layer DNA, and claim boundaries as separate evidence identities tied to the same patch.
11. Write the terminal lifecycle states to the out-of-repository immutable release receipt and final report, not the archived task file. Perform the predictive-KB postflight and record one structured observation only if the work exposed a reusable lesson, route gap, miss, or card weakness.
