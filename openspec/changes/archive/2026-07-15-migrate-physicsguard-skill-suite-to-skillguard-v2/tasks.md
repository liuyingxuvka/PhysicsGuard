## 1. Inventory and parent mesh

- [x] 1.1 Re-read all ten maintained SKILL.md files and native commands, then freeze a machine-readable inventory of target skill id, native owner, route/check, inputs, outputs, state/side effects, obligations, and claim boundary
- [x] 1.2 Add a PhysicsGuard skill-suite FlowGuard ModelMesh with complete child partition, shared-kernel/read-only relations, affected siblings, reattachment contracts, and closure tokens
- [x] 1.3 Add mesh known-bads for a missing child, duplicate owner, stale child receipt, wrong child id, unconsumed output, unsafe overlap, and locally green but unreattached child
- [x] 1.4 Bind the current primary dataset-validation skill as a required affected sibling and block missing, stale, or relabeled primary evidence

## 2. Target-native receipt adapters

- [x] 2.1 Add or refine the `physicsguard.ai-debugging` adapter to bind model-understanding, mapped evidence, validation depth, unresolved findings, and scoped audit result
- [x] 2.2 Add or refine the `physicsguard.audit-closure` adapter to bind required closure checks, blockers, stale/skipped evidence, validation depth, predictive rollout when applicable, and safe claim boundary
- [x] 2.3 Add or refine the `physicsguard.candidate-model-blueprint` adapter to bind validated hierarchy, block readiness, mapped parameters/signals, rollout boundary, and generation eligibility
- [x] 2.4 Add or refine the `physicsguard.model-library` adapter to bind the selected asset/profile/testbench inventory, compatibility evidence, gap gate, validation receipt, and bounded reuse scope
- [x] 2.5 Add or refine the `physicsguard.model-understanding-preflight` adapter to bind visible symptom, physical boundary, subsystem inventory, required signals, assumptions, and unresolved access gaps
- [x] 2.6 Add or refine the `physicsguard.project-adoption` adapter to bind current project records, supported toolchain, discovered native artifacts, blockers, and required revalidation
- [x] 2.7 Add or refine the `physicsguard.project-evidence-registry` adapter to bind declared/discovered/required/excluded files, binding edges, role coverage, critical gaps, and project evidence scope
- [x] 2.8 Add or refine the `physicsguard.signal-mapping-review` adapter to bind every governed mapping, unit/conversion/revision evidence, temporal coverage, confidence/review blockers, and target variables
- [x] 2.9 Add or refine the `physicsguard.test-file-contract-review` adapter to bind all governed fields/files, units, timing, testbench/model binding, per-signal depth, mapping evidence, and project-level gaps
- [x] 2.10 Preserve exact per-object obligation evidence plus every available and evaluated timepoint identity/coordinate and content fingerprint in the target-native receipt; counts alone are not sufficient evidence
- [x] 2.11 Replace blanket maintenance rerun instructions with current-V2-only authority, exact affected-check ownership, immutable receipt reuse, and one post-freeze final full gate

## 3. PhysicsGuard-owned purpose proof and generic SkillGuard contracts

- [x] 3.1 Author one route-specific V2 `contract-source.json` for each maintained skill with exact owner/check/obligation identities, typed evidence domains, dynamic depth providers, and non-overlapping native ranges
- [x] 3.2 Add target-owned positive and genuinely shallow fixtures for every maintained skill; ensure each shallow case omits one important native obligation and fails through the native evaluator
- [x] 3.3 Add focused migration tests for complete inventories, per-object depth, wrong target/owner/run/route, generic obligations, fixture-as-production, stale/missing native artifacts, catalog-only expansion, mechanical or overlapping ranges, and dishonest expected labels
- [x] 3.4 Add source-only portability tests and scheduled-production contract tests without allowing capability fixtures to satisfy production closure
- [x] 3.4b Replace fixture-derived production constructors with current-project artifact/semantic-row discovery, per-series timepoint denominators, adapter-side canonical-root rediscovery, exact semantic evidence bindings, and generic-placeholder/synchronized-shrink known-bads
- [x] 3.4a Add a target-ownership negative proving that an identity present only in the generic supervisor request cannot replace the package-owned production identity
- [x] 3.5 Add the suite checker and installed-skill checker coverage for exact V2 trio presence, former V1 residual absence, source/install byte parity, and installation receipt currentness
- [x] 3.6 Move purpose, external-universe, semantic-oracle, and prevented-failure ownership out of SkillGuard-specific schemas into a PhysicsGuard-native guard-model contract for all ten maintained skills
- [x] 3.7 Require the guard-model contract to be frozen before candidate construction, with no optional mode or alternate shallow route
- [x] 3.8 Replace positive/shallow calibration with mandatory known-good proof plus one exact known-bad proof for every declared prevented failure class; bind every blocked result to the declared failure id and finding code
- [x] 3.9 Replace obsolete `depth_profile`, `calibration`, `integration_mode`, target classification, and special evidence-output fields with the current generic SkillGuard declared-check shape
- [x] 3.10 Add known-bads for an undeclared failure, unproved failure, wrong finding code, non-blocking bad case, candidate authored before purpose freeze, and a generic SkillGuard semantic decision
- [x] 3.11 Update all ten SKILL.md prompts so every real model instance follows the fixed declaration -> candidate -> good proof -> all bad proofs -> native receipt -> generic supervision chain

## 4. Shared SkillGuard freeze and generated artifacts

- [x] 4.1 Consume and record the frozen canonical/installed SkillGuard source, runtime, schema, and installation interface identity before generation or supervision
- [x] 4.2 Compile all ten current V2 sources into exact `compiled-contract.json` and `check-manifest.json` artifacts with no V1 fallback path
- [x] 4.3 Freeze one fixed-input validation plan assigning each native/calibration/mesh/install check exactly one execution owner and one persistent receipt root

## 5. Fixed-input verification, retirement, and installation

- [x] 5.1 Run the affected native, purpose-contract, known-good, per-failure known-bad, migration-known-bad, FlowGuard mesh, and source-only portability checks once on the frozen source/toolchain snapshot; fix failures before proceeding
- [x] 5.2 Run scheduled-production supervision for every maintained skill and issue exact child terminal-success receipts plus one parent mesh receipt without background resume or duplicate owners
- [x] 5.3 Remove former V1 manifests, work contracts, generic checkers, mutable reports/evidence/ledgers, caches, and fallback text from all ten source skills; final child/parent closure remains gated separately below
- [x] 5.3a Apply the expanded retirement inventory, reject all prior two-file-only completion receipts, and issue new source/install completion receipts only after the complete residual scan is clean
- [x] 5.4 Transactionally install all ten skills, verify source/install hashes and installation currentness replay through issuance, terminal closure, parent consumption, and replay, then confirm former V1 residuals are absent in installed copies
- [x] 5.5 Emit the portable SkillGuard parent receipt reference required by `verification-contract.yaml`; ensure incomplete, stale, tampered, or identity-mismatched receipts fail closed and never execute an owner
- [x] 5.6 Run strict OpenSpec validation and receipt-only verification, archive the change only after every task and evidence gate passes, and refresh the global router once after all managed prompts and installed hashes freeze
