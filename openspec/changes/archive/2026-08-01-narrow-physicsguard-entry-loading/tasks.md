## 1. Freeze Current Authority and Route Content

- [x] 1.1 Record the ten current skill, native owner, native route, native check, prompt, and installed-package identities without touching peer-owned FlowGuard evidence.
- [x] 1.2 Preserve each skill's current detailed native workflow in one target-local `references/native-route-protocol.md` before contracting the entry prompt.

## 2. Generate Narrow Direct Entrypoints

- [x] 2.1 Extend `scripts/upgrade_purpose_contracts.py` with reviewed direct/composite route metadata and generate one current `references/route-capsule.json` per skill.
- [x] 2.2 Make the generator write complete target-specific `references/native-depth-and-purpose.md` artifacts preserving six-family depth, purpose-before-candidate, frozen prediction, `model_miss`, typed regression, independent holdout, predictive rollout, and exact terminals.
- [x] 2.3 Make the generator write complete `references/template-pack-routing.md` artifacts and remove the full VTP block from every entry prompt.
- [x] 2.4 Generate compact ten-skill `SKILL.md` entry maps with exact accept/reject/handoff boundaries, minimal workflow, and conditional one-level reference pointers.
- [x] 2.5 Regenerate all ten `agents/openai.yaml` files with route-specific `$skill-id` default prompts and no broad-route-first wording.

## 3. Add Executable Prompt-Loading Governance

- [x] 3.1 Add an author-side FlowGuard prompt-load graph covering metadata, compact entry, capsule, route protocol, depth protocol, and template protocol edges for all ten skills.
- [x] 3.2 Add known-bad and focused tests for owner/route drift, broad-route capture, eager reference loading, missing/undeclared/cross-skill references, stale hashes, and unreachable deep obligations.
- [x] 3.3 Update the existing PhysicsGuard skill-suite mesh/checker with the narrow-entry observable contract and current 0.15.1 dependency identity.

## 4. Refresh Author Contracts and Version Identity

- [x] 4.1 Regenerate all ten guard-model/runtime requirement artifacts and SkillGuard source contracts from the single current generator.
- [x] 4.2 Compile all ten `contract-source.json`, `compiled-contract.json`, and `check-manifest.json` trios under SkillGuard author supervision without starting the final full maintenance-unit gate.
- [x] 4.3 Update `VERSION`, `pyproject.toml`, package version source, and `CHANGELOG.md` to 0.15.1.

## 5. Verify the Affected Snapshot

- [x] 5.1 Validate the OpenSpec change and all ten skill/openai/reference/capsule shapes.
- [x] 5.2 Run focused prompt-load, route-capsule, task-local deepening, guard-model, version, and FlowGuard suite-model regressions.
- [x] 5.3 Run SkillGuard maintainer audit and affected declared checks only; keep skipped final full validation, installation, Git commit/push/tag, and release explicitly outside the claim.
- [x] 5.4 Recheck peer writes and repository status, perform the Predictive KB postflight, and record exact residual blockers and claim boundary.
