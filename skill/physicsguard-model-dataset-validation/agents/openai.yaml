interface:
  display_name: "PhysicsGuard Dataset Validation"
  short_description: "Validate models against exact project datasets"
  default_prompt: "Use $physicsguard-model-dataset-validation directly; consume the current affected or whole blueprint projection and report per-element obligations, validation modes, validity, residual/oracle, evidence fingerprints, and unsupported claims."
