interface:
  display_name: "PhysicsGuard Model Library"
  short_description: "Check bounded PhysicsGuard model reuse"
  default_prompt: "Use $physicsguard-model-library directly; index or select assets by current blueprint fingerprint, obligation, profile, validity boundary, testbench, receipt, and verified reuse limits, never by loose similarity."
