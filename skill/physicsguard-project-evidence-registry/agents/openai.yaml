interface:
  display_name: "PhysicsGuard Evidence Registry"
  short_description: "Map one project's PhysicsGuard evidence"
  default_prompt: "Use $physicsguard-project-evidence-registry directly for its native PhysicsGuard Project Evidence Registry route; confirm the route capsule, run only the target-owned checks required by the exact scope, and keep the result inside current evidence."
