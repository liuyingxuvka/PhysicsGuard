interface:
  display_name: "PhysicsGuard Evidence Registry"
  short_description: "Map one project's PhysicsGuard evidence"
  default_prompt: "Use $physicsguard-project-evidence-registry directly; bind current evidence/resource identities and freshness to exact blueprint elements in the summary or affected slice, and return missing or stale gaps without a project-wide pass."
