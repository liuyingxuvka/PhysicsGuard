interface:
  display_name: "PhysicsGuard Test File Contract"
  short_description: "Check concrete test-file coverage contracts"
  default_prompt: "Use $physicsguard-test-file-contract-review directly; bind deterministic files, fields, datasets, and testbench I/O to the exact affected blueprint interfaces, state, obligations, and evidence, with no full-blueprint fallback."
