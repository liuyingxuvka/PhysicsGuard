interface:
  display_name: "DataBank Workflow"
  short_description: "Guard-neutral database evidence ledger"
  default_prompt: "Use $databank-workflow to build or audit a project evidence database without copying raw data."

