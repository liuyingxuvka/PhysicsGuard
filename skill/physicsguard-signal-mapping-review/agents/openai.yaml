interface:
  display_name: "PhysicsGuard Signal Mapping"
  short_description: "Review exact signal and parameter mappings"
  default_prompt: "Use $physicsguard-signal-mapping-review directly on the exact affected interface slice; validate source/target identity, units, frames, conversion, time semantics, revision, evidence, and downstream blueprint consumers."
