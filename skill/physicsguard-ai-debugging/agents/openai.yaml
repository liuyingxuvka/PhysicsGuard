interface:
  display_name: "PhysicsGuard AI Debugging"
  short_description: "Coordinate mixed PhysicsGuard debugging routes"
  default_prompt: "Use $physicsguard-ai-debugging directly for its native PhysicsGuard AI Debugging route; confirm the route capsule, run only the target-owned checks required by the exact scope, and keep the result inside current evidence."
