interface:
  display_name: "PhysicsGuard AI Debugging"
  short_description: "Coordinate mixed PhysicsGuard debugging routes"
  default_prompt: "Use $physicsguard-ai-debugging only for genuinely mixed routing; consume current blueprint summary/affected/reverse trace, delegate to the minimum direct owners, and never become a second blueprint author or reviewer."
