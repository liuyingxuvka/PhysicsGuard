interface:
  display_name: "PhysicsGuard Audit Closure"
  short_description: "Close bounded PhysicsGuard audit claims"
  default_prompt: "Use $physicsguard-audit-closure directly; consume the exact whole or affected review and any in-memory projection status, preserve identity-only gaps, and keep interpretation separate from current native execution and closure."
