schema: physicsguard.project_formula.v1
module_type: RadiativeHeatTransferModule
owner: physicsguard.module_semantics.source_first.RadiativeHeatTransferModule
purpose: Source-first reconstruction of the exact maintained implementation, configuration, scenario ports, state roles, residuals,
  and protected failure.
authoring_status: source_first_reconstruction_pending_independent_review
separate_review_status: pending
physical_claim_licensed: false
direction_scope: exact_instantiation_source_first_reconstruction
relation_directionality: direction_neutral
configuration:
- name: Q_dot_initial_guess
  dimension: power
  unit: W
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''Q_dot_initial_guess'', 1000.0), ''Q_dot_initial_guess'')'
- name: Q_dot_lower_bound
  dimension: power
  unit: W
  required: false
  default: -100000000.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''Q_dot_lower_bound'', -100000000.0), ''Q_dot_lower_bound'')'
- name: Q_dot_scale
  dimension: power
  unit: W
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: positive_float(parameters.get(''Q_dot_scale'', 1000.0), ''Q_dot_scale'')'
- name: Q_dot_upper_bound
  dimension: power
  unit: W
  required: false
  default: 100000000.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''Q_dot_upper_bound'', 100000000.0), ''Q_dot_upper_bound'')'
- name: T_cold_initial_guess
  dimension: temperature
  unit: K
  required: false
  default: 300.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''T_cold_initial_guess'', 300.0), ''T_cold_initial_guess'')'
- name: T_cold_lower_bound
  dimension: temperature
  unit: K
  required: false
  default: 100.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''T_cold_lower_bound'', 100.0), ''T_cold_lower_bound'')'
- name: T_cold_scale
  dimension: temperature
  unit: K
  required: false
  default: 100.0
  constraints:
  - 'Maintained source guard: positive_float(parameters.get(''T_cold_scale'', 100.0), ''T_cold_scale'')'
- name: T_cold_upper_bound
  dimension: temperature
  unit: K
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''T_cold_upper_bound'', 1000.0), ''T_cold_upper_bound'')'
- name: T_hot_initial_guess
  dimension: temperature
  unit: K
  required: false
  default: 400.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''T_hot_initial_guess'', 400.0), ''T_hot_initial_guess'')'
- name: T_hot_lower_bound
  dimension: temperature
  unit: K
  required: false
  default: 100.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''T_hot_lower_bound'', 100.0), ''T_hot_lower_bound'')'
- name: T_hot_scale
  dimension: temperature
  unit: K
  required: false
  default: 100.0
  constraints:
  - 'Maintained source guard: positive_float(parameters.get(''T_hot_scale'', 100.0), ''T_hot_scale'')'
- name: T_hot_upper_bound
  dimension: temperature
  unit: K
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: finite_float(parameters.get(''T_hot_upper_bound'', 1000.0), ''T_hot_upper_bound'')'
- name: area_m2
  dimension: area
  unit: m^2
  required: true
  default: null
  constraints:
  - 'Maintained source guard: required_positive(parameters, ''area_m2'')'
- name: emissivity
  dimension: dimensionless
  unit: '1'
  required: true
  default: null
  constraints:
  - 'Maintained source guard: required(parameters, ''emissivity'')'
- name: residual_scale_W
  dimension: power
  unit: W
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: positive_float(parameters.get(''residual_scale_W'', 1000.0), ''residual_scale_W'')'
scenario_inputs:
- name: T_hot_K
  dimension: temperature
  unit: K
  role: input
- name: T_cold_K
  dimension: temperature
  unit: K
  role: input
scenario_outputs:
- name: Q_dot_W
  dimension: power
  unit: W
  role: output
residuals:
- name: radiative_heat_transfer
  expression: Q_dot_W - emissivity * 5.670374419e-8 * area_m2 * (T_hot_K ** 4 - T_cold_K ** 4)
  dimension: power
  unit: W
  dimensional_derivation: The exact maintained-source relation is normalized in W; physical meaning remains pending separate
    review.
  scale_expression: residual_scale_W
  zero_condition: The exact source-first relation radiative_heat_transfer evaluates to zero.
constraints:
  constructor:
  - predicate: residual_scale_W > 0
    guard_signature: positive_float(parameters.get('residual_scale_W', 1000.0), 'residual_scale_W')
    failure_type: ValueError
    message_selector: residual_scale_W
    on_violation: Reject non-positive residual_scale_W through the maintained source guard.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: 1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: fail
    - kind: outside
      inputs:
        residual_scale_W: -1.0
      expected: fail
  evaluation: []
regions:
  valid:
  - predicate: residual_scale_W > 0
    meaning: The protected maintained constructor guard accepts this region.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: 1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: fail
    - kind: outside
      inputs:
        residual_scale_W: -1.0
      expected: fail
  invalid:
  - predicate: not (residual_scale_W > 0)
    meaning: The protected maintained constructor guard rejects this region.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: -1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: pass
    - kind: outside
      inputs:
        residual_scale_W: 1.0
      expected: fail
assumptions:
- This reconstruction describes the exact bound current source and example only.
- No physical truth or cross-target generalization is licensed by source agreement.
validity:
- Use only while the bound implementation, exact instantiation, formula, ports, and tests retain matching fingerprints.
invariants:
- All maintained constructor parameters have explicit source provenance.
- Scenario ports exactly cover every current local declaration and referenced external variable.
effects:
- Read the exact bound scenario ports and emit 1 named residual record(s) without mutating the solver vector.
postconditions:
- Every emitted record keeps its source name, role, positive scale, diagnostic key, and source-bound arithmetic.
termination: Finite algebraic source-first evaluation terminates without retry or hidden iteration.
behavior_contract:
  kind: residual_equations
  residuals:
  - radiative_heat_transfer
  input_ports:
  - T_hot_K
  - T_cold_K
  output_ports:
  - Q_dot_W
oracle_cases:
- case_id: exact_bound_current_source
  inputs:
    area_m2: 1.0
    emissivity: 1.0
    residual_scale_W: 1000.0
    T_hot_lower_bound: 100.0
    T_hot_upper_bound: 1000.0
    T_hot_initial_guess: 400.0
    T_hot_scale: 100.0
    T_cold_lower_bound: 100.0
    T_cold_upper_bound: 1000.0
    T_cold_initial_guess: 300.0
    T_cold_scale: 100.0
    Q_dot_lower_bound: -100000000.0
    Q_dot_upper_bound: 100000000.0
    Q_dot_initial_guess: 1000.0
    Q_dot_scale: 1000.0
    T_hot_K: 400.0
    T_cold_K: 300.0
    Q_dot_W: 1000.0
  expected:
    radiative_heat_transfer: 7.684476675000042
  tolerance: 1.0e-09
claim_boundary: This is an unlicensed source-first reconstruction for one exact maintained instantiation. It does not prove
  physical correctness, external FMI coverage, or arbitrary-target reconstruction until separate evidence accepts those claims.
