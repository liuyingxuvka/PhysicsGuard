schema: physicsguard.project_formula.v1
module_type: ElectrochemicalStackPowerModule
owner: physicsguard.module_semantics.source_first.ElectrochemicalStackPowerModule
purpose: Source-first reconstruction of the exact maintained implementation, configuration, scenario ports, state roles, residuals,
  and protected failure.
authoring_status: source_first_reconstruction_pending_independent_review
separate_review_status: pending
physical_claim_licensed: false
direction_scope: exact_instantiation_source_first_reconstruction
relation_directionality: direction_neutral
configuration:
- name: P_initial_guess
  dimension: power
  unit: W
  required: false
  default: 10000.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''P_initial_guess'', 10000.0), ''P_initial_guess'')'
- name: P_lower_bound
  dimension: power
  unit: W
  required: false
  default: 0.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''P_lower_bound'', 0.0), ''P_lower_bound'')'
- name: P_scale
  dimension: power
  unit: W
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: _positive_float(parameters.get(''P_scale'', 1000.0), ''P_scale'')'
- name: P_upper_bound
  dimension: power
  unit: W
  required: false
  default: 100000000.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''P_upper_bound'', 100000000.0), ''P_upper_bound'')'
- name: V_cell_initial_guess
  dimension: voltage
  unit: V
  required: false
  default: 0.7
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''V_cell_initial_guess'', 0.7), ''V_cell_initial_guess'')'
- name: V_cell_lower_bound
  dimension: voltage
  unit: V
  required: false
  default: 0.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''V_cell_lower_bound'', 0.0), ''V_cell_lower_bound'')'
- name: V_cell_scale
  dimension: voltage
  unit: V
  required: false
  default: 1.0
  constraints:
  - 'Maintained source guard: _positive_float(parameters.get(''V_cell_scale'', 1.0), ''V_cell_scale'')'
- name: V_cell_upper_bound
  dimension: voltage
  unit: V
  required: false
  default: 3.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''V_cell_upper_bound'', 3.0), ''V_cell_upper_bound'')'
- name: current_initial_guess
  dimension: electric_current
  unit: A
  required: false
  default: 100.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''current_initial_guess'', 100.0), ''current_initial_guess'')'
- name: current_lower_bound
  dimension: electric_current
  unit: A
  required: false
  default: 0.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''current_lower_bound'', 0.0), ''current_lower_bound'')'
- name: current_scale
  dimension: electric_current
  unit: A
  required: false
  default: 100.0
  constraints:
  - 'Maintained source guard: _positive_float(parameters.get(''current_scale'', 100.0), ''current_scale'')'
- name: current_upper_bound
  dimension: electric_current
  unit: A
  required: false
  default: 5000.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''current_upper_bound'', 5000.0), ''current_upper_bound'')'
- name: n_cells
  dimension: count
  unit: count
  required: true
  default: null
  constraints:
  - 'Maintained source guard: _required_positive(parameters, ''n_cells'')'
- name: residual_scale_W
  dimension: power
  unit: W
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: _positive_float(parameters.get(''residual_scale_W'', 1000.0), ''residual_scale_W'')'
scenario_inputs:
- name: V_cell_V
  dimension: voltage
  unit: V
  role: input
- name: current_A
  dimension: electric_current
  unit: A
  role: input
scenario_outputs:
- name: P_stack_W
  dimension: power
  unit: W
  role: output
residuals:
- name: electrochemical_stack_power
  expression: P_stack_W - n_cells * V_cell_V * current_A
  dimension: power
  unit: W
  dimensional_derivation: The exact maintained-source relation is normalized in W; physical meaning remains pending separate
    review.
  scale_expression: residual_scale_W
  zero_condition: The exact source-first relation electrochemical_stack_power evaluates to zero.
constraints:
  constructor:
  - predicate: residual_scale_W > 0
    guard_signature: _positive_float(parameters.get('residual_scale_W', 1000.0), 'residual_scale_W')
    failure_type: ValueError
    message_selector: residual_scale_W
    on_violation: Reject non-positive residual_scale_W through the maintained source guard.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: 1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: fail
    - kind: outside
      inputs:
        residual_scale_W: -1.0
      expected: fail
  evaluation: []
regions:
  valid:
  - predicate: residual_scale_W > 0
    meaning: The protected maintained constructor guard accepts this region.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: 1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: fail
    - kind: outside
      inputs:
        residual_scale_W: -1.0
      expected: fail
  invalid:
  - predicate: not (residual_scale_W > 0)
    meaning: The protected maintained constructor guard rejects this region.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: -1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: pass
    - kind: outside
      inputs:
        residual_scale_W: 1.0
      expected: fail
assumptions:
- This reconstruction describes the exact bound current source and example only.
- No physical truth or cross-target generalization is licensed by source agreement.
validity:
- Use only while the bound implementation, exact instantiation, formula, ports, and tests retain matching fingerprints.
invariants:
- All maintained constructor parameters have explicit source provenance.
- Scenario ports exactly cover every current local declaration and referenced external variable.
effects:
- Read the exact bound scenario ports and emit 1 named residual record(s) without mutating the solver vector.
postconditions:
- Every emitted record keeps its source name, role, positive scale, diagnostic key, and source-bound arithmetic.
termination: Finite algebraic source-first evaluation terminates without retry or hidden iteration.
behavior_contract:
  kind: residual_equations
  residuals:
  - electrochemical_stack_power
  input_ports:
  - V_cell_V
  - current_A
  output_ports:
  - P_stack_W
oracle_cases:
- case_id: exact_bound_current_source
  inputs:
    n_cells: 400.0
    residual_scale_W: 1000.0
    V_cell_lower_bound: 0.0
    V_cell_upper_bound: 3.0
    V_cell_initial_guess: 0.7
    V_cell_scale: 1.0
    current_lower_bound: 0.0
    current_upper_bound: 5000.0
    current_initial_guess: 100.0
    current_scale: 100.0
    P_lower_bound: 0.0
    P_upper_bound: 100000000.0
    P_initial_guess: 28000.0
    P_scale: 1000.0
    V_cell_V: 0.7
    current_A: 100.0
    P_stack_W: 28000.0
  expected:
    electrochemical_stack_power: 0.0
  tolerance: 1.0e-09
claim_boundary: This is an unlicensed source-first reconstruction for one exact maintained instantiation. It does not prove
  physical correctness, external FMI coverage, or arbitrary-target reconstruction until separate evidence accepts those claims.
