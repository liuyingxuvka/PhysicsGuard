schema: physicsguard.project_formula.v1
module_type: AggregateThermalBalanceModule
owner: physicsguard.module_semantics.source_first.AggregateThermalBalanceModule
purpose: Source-first reconstruction of the exact maintained implementation, configuration, scenario ports, state roles, residuals,
  and protected failure.
authoring_status: source_first_reconstruction_pending_independent_review
separate_review_status: pending
physical_claim_licensed: false
direction_scope: exact_instantiation_source_first_reconstruction
relation_directionality: direction_neutral
configuration:
- name: heat_in_variables
  dimension: enum
  unit: enum
  required: false
  default: []
  constraints:
  - 'Maintained source guard: _variable_list(parameters, ''heat_in_variables'', required=False)'
- name: heat_out_variables
  dimension: enum
  unit: enum
  required: false
  default: []
  constraints:
  - 'Maintained source guard: _variable_list(parameters, ''heat_out_variables'', required=False)'
- name: heat_storage_rate_variable
  dimension: enum
  unit: enum
  required: false
  default: null
  constraints:
  - 'Maintained source guard: _optional_variable(parameters.get(''heat_storage_rate_variable''), ''heat_storage_rate_variable'')'
- name: residual_scale_W
  dimension: power
  unit: W
  required: false
  default: 1000.0
  constraints:
  - 'Maintained source guard: _positive_float(parameters.get(''residual_scale_W'', 1000.0), ''residual_scale_W'')'
- name: target_storage_rate_W
  dimension: power
  unit: W
  required: false
  default: 0.0
  constraints:
  - 'Maintained source guard: _finite_float(parameters.get(''target_storage_rate_W'', 0.0), ''target_storage_rate_W'')'
scenario_inputs:
- name: heat_in_0
  dimension: power
  unit: W
  role: input
  source_kind: external
  source_attribute: heat_in
  source_reference: electronics_heat_generation.value
  source_index: 0
- name: heat_in_1
  dimension: power
  unit: W
  role: input
  source_kind: external
  source_attribute: heat_in
  source_reference: absorbed_solar_heat.value
  source_index: 1
- name: heat_out_0
  dimension: power
  unit: W
  role: input
  source_kind: external
  source_attribute: heat_out
  source_reference: radiator_heat_rejection.value
  source_index: 0
- name: heat_out_1
  dimension: power
  unit: W
  role: input
  source_kind: external
  source_attribute: heat_out
  source_reference: thermal_storage_rate.value
  source_index: 1
scenario_outputs: []
residuals:
- name: aggregate_thermal_balance
  expression: heat_in_0 + heat_in_1 - heat_out_0 - heat_out_1 - target_storage_rate_W
  dimension: power
  unit: W
  dimensional_derivation: The exact maintained-source relation is normalized in W; physical meaning remains pending separate
    review.
  scale_expression: residual_scale_W
  zero_condition: The exact source-first relation aggregate_thermal_balance evaluates to zero.
constraints:
  constructor:
  - predicate: residual_scale_W > 0
    guard_signature: _positive_float(parameters.get('residual_scale_W', 1000.0), 'residual_scale_W')
    failure_type: ValueError
    message_selector: residual_scale_W
    on_violation: Reject non-positive residual_scale_W through the maintained source guard.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: 1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: fail
    - kind: outside
      inputs:
        residual_scale_W: -1.0
      expected: fail
  evaluation: []
regions:
  valid:
  - predicate: residual_scale_W > 0
    meaning: The protected maintained constructor guard accepts this region.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: 1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: fail
    - kind: outside
      inputs:
        residual_scale_W: -1.0
      expected: fail
  invalid:
  - predicate: not (residual_scale_W > 0)
    meaning: The protected maintained constructor guard rejects this region.
    cases:
    - kind: inside
      inputs:
        residual_scale_W: -1.0
      expected: pass
    - kind: boundary
      inputs:
        residual_scale_W: 0.0
      expected: pass
    - kind: outside
      inputs:
        residual_scale_W: 1.0
      expected: fail
assumptions:
- This reconstruction describes the exact bound current source and example only.
- No physical truth or cross-target generalization is licensed by source agreement.
validity:
- Use only while the bound implementation, exact instantiation, formula, ports, and tests retain matching fingerprints.
invariants:
- All maintained constructor parameters have explicit source provenance.
- Scenario ports exactly cover every current local declaration and referenced external variable.
effects:
- Read the exact bound scenario ports and emit 1 named residual record(s) without mutating the solver vector.
postconditions:
- Every emitted record keeps its source name, role, positive scale, diagnostic key, and source-bound arithmetic.
termination: Finite algebraic source-first evaluation terminates without retry or hidden iteration.
behavior_contract:
  kind: residual_equations
  residuals:
  - aggregate_thermal_balance
  input_ports:
  - heat_in_0
  - heat_in_1
  - heat_out_0
  - heat_out_1
  output_ports: []
oracle_cases:
- case_id: exact_bound_current_source
  inputs:
    heat_in_variables:
    - electronics_heat_generation.value
    - absorbed_solar_heat.value
    heat_out_variables:
    - radiator_heat_rejection.value
    - thermal_storage_rate.value
    heat_storage_rate_variable: null
    target_storage_rate_W: 0.0
    residual_scale_W: 100.0
    heat_in_0: 1.0
    heat_in_1: 1.0
    heat_out_0: 1.0
    heat_out_1: 1.0
  expected:
    aggregate_thermal_balance: 0.0
  tolerance: 1.0e-09
claim_boundary: This is an unlicensed source-first reconstruction for one exact maintained instantiation. It does not prove
  physical correctness, external FMI coverage, or arbitrary-target reconstruction until separate evidence accepts those claims.
