# PhysicsGuard system audit/model blueprint
# Purpose: foundation thermal capacitance rate.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/foundation/thermal_capacitance_rate.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: foundation_thermal_capacitance_rate
components:
  - id: cap
    type: ThermalCapacitanceRateModule
    parameters:
      C_J_K: 500.0
boundaries:
  - variable: cap.dT_dt_K_s
    value: 2.0
    unit: K/s
solver:
  tolerance: 1.0e-8
  audit_threshold: 1.0
