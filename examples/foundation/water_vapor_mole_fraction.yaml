# PhysicsGuard system audit/model blueprint
# Purpose: foundation water vapor mole fraction.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/foundation/water_vapor_mole_fraction.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: foundation_water_vapor_mole_fraction
components:
  - id: vap
    type: WaterVaporMoleFractionModule
    parameters: {}
boundaries:
  - variable: vap.p_total_Pa
    value: 101325.0
    unit: Pa
  - variable: vap.p_vapor_Pa
    value: 2000.0
    unit: Pa
solver:
  tolerance: 1.0e-8
  audit_threshold: 1.0
