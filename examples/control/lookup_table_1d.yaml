# PhysicsGuard system audit/model blueprint
# Purpose: control lookup table 1d.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/control/lookup_table_1d.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: control_lookup_table_1d
components:
  - id: table
    type: LookupTable1DModule
    parameters:
      x_points: [0.0, 1.0, 2.0]
      y_points: [0.0, 10.0, 20.0]
boundaries:
  - variable: table.x
    value: 1.5
solver:
  tolerance: 1.0e-8
  audit_threshold: 1.0
