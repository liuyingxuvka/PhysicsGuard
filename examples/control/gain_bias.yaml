# PhysicsGuard system audit/model blueprint
# Purpose: control gain bias.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/control/gain_bias.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: control_gain_bias
components:
  - id: gain
    type: GainBiasModule
    parameters:
      gain: 2.0
      bias: 1.0
boundaries:
  - variable: gain.x
    value: 2.0
solver:
  tolerance: 1.0e-8
  audit_threshold: 1.0
