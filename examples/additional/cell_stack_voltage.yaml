# PhysicsGuard system audit/model blueprint
# Purpose: additional cell stack voltage.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/additional/cell_stack_voltage.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: additional_cell_stack_voltage
components:
  - id: stack
    type: CellVoltageStackVoltageModule
    parameters:
      n_cells: 400.0
boundaries:
  - variable: stack.V_cell_V
    value: 0.7
    unit: V
solver:
  tolerance: 1.0e-8
  audit_threshold: 1.0
