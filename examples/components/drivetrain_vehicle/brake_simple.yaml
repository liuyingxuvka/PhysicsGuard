# PhysicsGuard system audit/model blueprint
# Purpose: component brake simple.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/components/drivetrain_vehicle/brake_simple.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: component_brake_simple
components:
  - id: brake
    type: BrakeSimpleModule
    parameters: {}
boundaries:
  - {variable: brake.brake_force_N, value: 1000.0}
  - {variable: brake.vehicle_speed_m_s, value: 20.0}
  - {variable: brake.brake_power_W, value: 20000.0}
solver: {tolerance: 1.0e-8, audit_threshold: 1.0}
