# PhysicsGuard system audit/model blueprint
# Purpose: component battery pack power.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/components/battery_hv/battery_pack_power.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: component_battery_pack_power
components:
  - id: pack
    type: BatteryPackPowerModule
    parameters: {}
boundaries:
  - {variable: pack.terminal_voltage_V, value: 350.0}
  - {variable: pack.current_A, value: 10.0}
  - {variable: pack.power_W, value: 3500.0}
solver: {tolerance: 1.0e-8, audit_threshold: 1.0}
