# PhysicsGuard system audit/model blueprint
# Purpose: component inverter simple.
# Repository: https://github.com/liuyingxuvka/PhysicsGuard
# Use with: python -m physicsguard.cli run examples/components/electric/inverter_simple.yaml --pretty
# Boundary: Low-fidelity SI-unit residual audit or blueprint only; not a high-fidelity solver, commercial-tool adapter, or reverse-engineered model.
system_name: component_inverter_simple
components:
  - id: inv
    type: InverterSimpleModule
    parameters: {}
boundaries:
  - variable: inv.P_dc_W
    value: 4000.0
  - variable: inv.efficiency
    value: 0.9
solver:
  tolerance: 1.0e-8
  audit_threshold: 1.0
